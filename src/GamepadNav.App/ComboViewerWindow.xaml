<Window x:Class="GamepadNav.App.ComboViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GamepadNav Shortcuts"
        Width="700" Height="460"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        ShowActivated="False" Focusable="False"
        Visibility="Hidden">
    <Border Background="#F2141420" CornerRadius="16" Padding="30,24" Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="26" ShadowDepth="0" Color="Black" Opacity="0.65"/>
        </Border.Effect>
        <StackPanel>
            <!-- Status header: mode indicator + dismiss hint -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Ellipse x:Name="StatusDot" Grid.Column="0" Width="16" Height="16"
                         Fill="#4CAF50" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" Grid.Column="1" Text="GamepadNav Active"
                           Foreground="White" FontSize="22" FontWeight="Bold"
                           FontFamily="Segoe UI" Margin="12,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="Back + B to close"
                           Foreground="#8A8A99" FontSize="15" FontFamily="Segoe UI"
                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>

            <!-- Two-column shortcut grid, built in code-behind for couch-readable rows -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel x:Name="LeftColumn" Grid.Column="0"/>
                <StackPanel x:Name="RightColumn" Grid.Column="2"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
